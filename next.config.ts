import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/next-js-landing-page',
  assetPrefix: '/next-js-landing-page/',
  trailingSlash: true,
};

export default nextConfig;
